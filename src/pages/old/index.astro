---
import { readFileSync } from "node:fs";
import path from "node:path";

const legacyHtml = readFileSync(
  path.join(process.cwd(), "src", "legacy", "old-index.html"),
  "utf8"
);
---

<Fragment set:html={legacyHtml} />
