# Cloudflare Pages reads this file during Git builds. Keep it aligned with the
# dashboard settings for the static prerendered TanStack output.

name = "serayawebsite1"
compatibility_date = "2026-05-05"
pages_build_output_dir = "dist/client"
